import { PontokPipePipe } from './pontok.pipe.pipe';

describe('PontokPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PontokPipePipe();
    expect(pipe).toBeTruthy();
  });
});
